import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.new',
  appName: 'new',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
